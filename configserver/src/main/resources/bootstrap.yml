spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ruobai-s/aispringcloud.git
          search-paths: config
          username: 2522100775@qq.com
          password: zyzonlyyou0809
      label: master
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
